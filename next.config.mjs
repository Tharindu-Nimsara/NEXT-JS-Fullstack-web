/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ["images.pexels.com"], // have to add domain of websites when You load image links from them
  },
};

export default nextConfig;
